BEGIN{
	FS = ",";
	# Change the delimeter used to seperate the data

	makeList = []
}


FNR > 1{ # skip the first kine as it is a header row
	total = 0; # initilize total to 0 for each entry
	for(i = 8; i <= NF; i++){ # start at 8 as that is where rank start
		total += $i;
	};
	print total;

	
}


END{


}
